import React from 'react';
import Navigation from '../Navigation/Navigation';

const Headers = () => {
    return (
        <div>
            <Navigation/>
        </div>
    );
};

export default Headers;